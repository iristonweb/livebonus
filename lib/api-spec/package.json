{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "orval --config ./orval.config.ts && node ../../scripts/check-generated-contracts.mjs --clean-only && pnpm -w exec tsc --build --force"
  },
  "devDependencies": {
    "orval": "^8.9.1"
  }
}
